# Add this block to your i3blocks config (~/.config/i3blocks/config), or
# merge it in with `include /path/to/percy/i3blocks.conf` if your i3blocks
# version supports the `include` directive.
[percy-screenshot]
command=$HOME/.local/bin/percy-screenshot-ctl.sh block
interval=5
markup=none
